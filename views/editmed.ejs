<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Medicine</title>
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
  <style>
    :root {
      --brand: #1abc9c;
      --brand-dark: #148f77;
    }

    body {
      background-color: #f4f6f8;
    }

    nav {
      background-color: var(--brand);
    }

    .card-panel {
      border-radius: 16px;
      box-shadow: 0 16px 30px rgba(0, 0, 0, 0.08);
      animation: fadeUp 0.6s ease;
    }

    .btn {
      background-color: var(--brand);
    }

    .btn:hover {
      background-color: var(--brand-dark);
    }

    .input-field input:focus + label {
      color: var(--brand) !important;
    }

    .input-field input:focus {
      border-bottom: 1px solid var(--brand) !important;
      box-shadow: 0 1px 0 0 var(--brand) !important;
    }

    @keyframes fadeUp {
      from { transform: translateY(16px); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }
  </style>
</head>
<body>
  <nav>
    <div class="nav-wrapper container">
      <a href="/admin/Medcines" class="brand-logo">💊 Pharmacy</a>
      <ul class="right hide-on-med-and-down">
        <li><a href="/admin/Medcines">View All</a></li>
        <li><a href="/logout">Logout</a></li>
      </ul>
    </div>
  </nav>

  <main class="container">
    <div class="row">
      <div class="col s12 m10 l6 offset-m1 offset-l3">
        <div class="card-panel white">
          <h4 class="center-align">Edit Medicine</h4>
          <form action="/edit/<%= mte._id %>" method="POST" enctype="multipart/form-data">
            <div class="input-field">
              <i class="material-icons prefix">medication</i>
              <input id="name" type="text" name="name" value="<%= mte.name %>" required />
              <label for="name" class="active">Medicine Name</label>
            </div>
            <div class="input-field">
              <i class="material-icons prefix">science</i>
              <input id="form" type="text" name="form" value="<%= mte.form %>" required />
              <label for="form" class="active">Form (tablet, syrup)</label>
            </div>
            <div class="input-field">
              <i class="material-icons prefix">attach_money</i>
              <input id="price" type="number" name="price" value="<%= mte.price %>" step="0.01" required />
              <label for="price" class="active">Price in $</label>
            </div>
            <div class="file-field input-field">
              <div class="btn">
                <span>Image</span>
                <input type="file" name="image" accept="image/*" />
              </div>
              <div class="file-path-wrapper">
                <input class="file-path validate" type="text" placeholder="Upload new image (optional)" />
              </div>
            </div>
            <div class="center-align">
              <button class="btn-large waves-effect waves-light" type="submit">
                <i class="material-icons left">save</i>
                Save Changes
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </main>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>
</html>
