<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard | PharmaPlus</title>
  
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  
  <script src="https://unpkg.com/@phosphor-icons/web"></script>

  <style>
    /* --- THEME VARIABLES --- */
    :root {
      --primary: #0d9488;
      --primary-dark: #0f766e;
      --bg-body: #f3f4f6;
      --bg-surface: #ffffff;
      --text-main: #111827;
      --text-muted: #6b7280;
      --border: #e5e7eb;
      --danger: #ef4444;
      --sidebar-width: 260px;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; outline: none; }

    body {
      font-family: 'Inter', sans-serif;
      background: var(--bg-body);
      color: var(--text-main);
      display: flex;
      min-height: 100vh;
    }

    /* --- SIDEBAR --- */
    aside {
      width: var(--sidebar-width);
      background: var(--bg-surface);
      border-right: 1px solid var(--border);
      position: fixed;
      height: 100vh;
      padding: 24px;
      display: flex;
      flex-direction: column;
      z-index: 10;
    }

    .brand {
      font-size: 1.25rem;
      font-weight: 700;
      color: var(--primary);
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 40px;
      text-decoration: none;
    }

    .nav-menu {
      list-style: none;
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .nav-link {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 12px 16px;
      color: var(--text-muted);
      text-decoration: none;
      border-radius: 8px;
      font-weight: 500;
      transition: all 0.2s;
    }

    .nav-link:hover { background: #f0fdfa; color: var(--primary); }
    .nav-link.active { background: #f0fdfa; color: var(--primary); font-weight: 600; }
    
    .nav-footer { margin-top: auto; }

    /* --- MAIN CONTENT --- */
    main {
      margin-left: var(--sidebar-width);
      flex: 1;
      padding: 32px 40px;
      max-width: 1600px;
    }

    /* --- HEADER & STATS --- */
    .header-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 32px;
    }

    .page-title { font-size: 1.8rem; font-weight: 700; }

    .stats-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 24px;
      margin-bottom: 40px;
    }

    .stat-card {
      background: var(--bg-surface);
      padding: 24px;
      border-radius: 12px;
      border: 1px solid var(--border);
      display: flex;
      align-items: center;
      gap: 20px;
    }

    .stat-icon {
      width: 48px; height: 48px;
      border-radius: 10px;
      background: #f0fdfa;
      color: var(--primary);
      display: flex; align-items: center; justify-content: center;
      font-size: 1.5rem;
    }

    .stat-info h4 { color: var(--text-muted); font-size: 0.9rem; font-weight: 500; }
    .stat-info p { font-size: 1.5rem; font-weight: 700; margin-top: 5px; }

    /* --- TABLE SECTION --- */
    .table-container {
      background: var(--bg-surface);
      border-radius: 12px;
      border: 1px solid var(--border);
      overflow: hidden;
    }

    .table-header {
      padding: 20px 24px;
      border-bottom: 1px solid var(--border);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .search-box {
      position: relative;
      width: 300px;
    }

    .search-box i {
      position: absolute; left: 12px; top: 50%; transform: translateY(-50%);
      color: var(--text-muted);
    }

    .search-input {
      width: 100%;
      padding: 10px 10px 10px 36px;
      border: 1px solid var(--border);
      border-radius: 8px;
      font-family: inherit;
    }

    .btn-add {
      background: var(--primary);
      color: white;
      text-decoration: none;
      padding: 10px 20px;
      border-radius: 8px;
      font-weight: 600;
      display: flex; align-items: center; gap: 8px;
      transition: 0.2s;
    }
    .btn-add:hover { background: var(--primary-dark); }

    /* --- DATA TABLE --- */
    table { width: 100%; border-collapse: collapse; }
    
    th {
      text-align: left;
      padding: 16px 24px;
      background: #f9fafb;
      font-size: 0.85rem;
      text-transform: uppercase;
      color: var(--text-muted);
      font-weight: 600;
      border-bottom: 1px solid var(--border);
    }

    td {
      padding: 16px 24px;
      border-bottom: 1px solid var(--border);
      font-size: 0.95rem;
      vertical-align: middle;
    }

    tr:last-child td { border-bottom: none; }
    tr:hover { background: #f8fafc; }

    .med-thumb {
      width: 40px; height: 40px;
      border-radius: 8px;
      object-fit: cover;
      background: #eee;
    }

    .price-badge {
      background: #ecfccb;
      color: #3f6212;
      padding: 4px 10px;
      border-radius: 99px;
      font-size: 0.85rem;
      font-weight: 600;
    }

    .action-group {
      display: flex;
      gap: 10px;
    }

    .btn-icon {
      border: 1px solid var(--border);
      background: white;
      color: var(--text-muted);
      width: 32px; height: 32px;
      border-radius: 6px;
      display: flex; align-items: center; justify-content: center;
      cursor: pointer;
      transition: 0.2s;
      text-decoration: none;
    }

    .btn-icon:hover { border-color: var(--primary); color: var(--primary); }
    .btn-icon.delete:hover { border-color: var(--danger); color: var(--danger); }

  </style>
</head>
<body>

  <aside>
    <a href="/user/Medcines" class="brand">
      <i class="ph-fill ph-first-aid-kit" style="font-size: 24px;"></i>
      PharmaPlus
    </a>

    <ul class="nav-menu">
      <li><a href="#" class="nav-link active"><i class="ph ph-squares-four"></i> Dashboard</a></li>
      <li><a href="/admin/Users" class="nav-link"><i class="ph ph-users"></i> Users</a></li>
      <li><a href="#" class="nav-link"><i class="ph ph-gear"></i> Settings</a></li>
    </ul>

    <div class="nav-footer">
      <a href="/logout" class="nav-link" style="color: var(--danger);">
        <i class="ph ph-sign-out"></i> Logout
      </a>
    </div>
  </aside>

  <main>
    
    <div class="header-row">
      <h1 class="page-title">Dashboard Overview</h1>
      <div style="display: flex; align-items: center; gap: 10px;">
        <span style="color: var(--text-muted);">Welcome back,<%= username%></span>
        <div style="width: 35px; height: 35px; background: var(--primary); color: white; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-weight: bold;"><%=username[0] %></div>
      </div>
    </div>

    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-icon"><i class="ph ph-pill"></i></div>
        <div class="stat-info">
          <h4>Total Medicines</h4>
          <p><%= meds.length %></p>
        </div>
      </div>

      <div class="stat-card">
        <div class="stat-icon" style="color: #2563eb; background: #eff6ff;"><i class="ph ph-currency-dollar"></i></div>
        <div class="stat-info">
          <h4>Inventory Value</h4>
          <p>$<%= meds.reduce((acc, med) => acc + med.price, 0).toLocaleString() %></p>
        </div>
      </div>

      <div class="stat-card">
        <div class="stat-icon" style="color: #ea580c; background: #fff7ed;"><i class="ph ph-warning"></i></div>
        <div class="stat-info">
          <h4>Low Stock Items</h4>
          <p>3</p> </div>
      </div>
    </div>

    <div class="table-container">
      
      <div class="table-header">
        <div class="search-box">
          <i class="ph ph-magnifying-glass"></i>
          <input type="text" id="tableSearch" class="search-input" placeholder="Search inventory...">
        </div>
        
        <a href="/admin/Medcines/add" class="btn-add">
          <i class="ph-bold ph-plus"></i> Add Medicine
        </a>
      </div>

      <table>
        <thead>
          <tr>
            <th width="80">Image</th>
            <th>Name</th>
            <th>Form / Category</th>
            <th>Price</th>
            <th style="text-align: right;">Actions</th>
          </tr>
        </thead>
        <tbody id="medTableBody">
          <% meds.forEach(med => { %>
            <tr class="med-row">
              <td>
                <% if (med.imageUrl) { %>
                  <img src="<%= med.imageUrl %>" class="med-thumb" alt="img">
                <% } else { %>
                  <img src="https://images.unsplash.com/photo-1584017911766-d451b3d0e843?auto=format&fit=crop&w=50&q=50" class="med-thumb">
                <% } %>
              </td>
              <td style="font-weight: 600; color: var(--text-main);" class="med-name">
                <%= med.name %>
              </td>
              <td style="color: var(--text-muted);" class="med-form">
                <%= med.form %>
              </td>
              <td>
                <span class="price-badge">$<%= med.price %></span>
              </td>
              <td>
                <div class="action-group" style="justify-content: flex-end;">
                  
                  <a href="/edit/<%= med._id %>" class="btn-icon" title="Edit">
                    <i class="ph ph-pencil-simple"></i>
                  </a>

                  <form action="/delete/<%= med._id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this medicine?');">
                    <button type="submit" class="btn-icon delete" title="Delete">
                      <i class="ph ph-trash"></i>
                    </button>
                  </form>

                </div>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
      
      <div id="noData" style="display: none; padding: 40px; text-align: center; color: var(--text-muted);">
        <i class="ph ph-magnifying-glass" style="font-size: 2rem; margin-bottom: 10px; display: block;"></i>
        No medicines found matching your search.
      </div>

    </div>

  </main>

  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const searchInput = document.getElementById('tableSearch');
      const tableRows = document.querySelectorAll('.med-row');
      const noData = document.getElementById('noData');

      searchInput.addEventListener('input', (e) => {
        const term = e.target.value.toLowerCase();
        let visibleCount = 0;

        tableRows.forEach(row => {
          // Select Name and Form columns for filtering
          const name = row.querySelector('.med-name').textContent.toLowerCase();
          const form = row.querySelector('.med-form').textContent.toLowerCase();

          if (name.includes(term) || form.includes(term)) {
            row.style.display = '';
            visibleCount++;
          } else {
            row.style.display = 'none';
          }
        });

        // Toggle Empty State
        noData.style.display = visibleCount === 0 ? 'block' : 'none';
      });
    });
  </script>

</body>
</html>