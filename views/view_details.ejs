<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= mtv.name %> | PharmaPlus</title>
  
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;800&display=swap" rel="stylesheet">
  <script src="https://unpkg.com/@phosphor-icons/web"></script>

  <style>
    :root {
      --primary: #0d9488;
      --brand-dark: #0f172a;
      --text-muted: #64748b;
      --glass: rgba(255, 255, 255, 0.85);
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: 'Plus Jakarta Sans', sans-serif;
      background: radial-gradient(circle at top right, #f0fdfa, #f8fafc);
      color: var(--brand-dark);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 40px 20px;
    }

    /* Floating Navigation */
    .back-nav {
      position: absolute; top: 40px; left: 40px;
      display: flex; align-items: center; gap: 8px;
      text-decoration: none; color: var(--text-muted);
      font-weight: 700; font-size: 0.8rem; letter-spacing: 1px;
      transition: 0.3s;
    }
    .back-nav:hover { color: var(--primary); transform: translateX(-5px); }

    /* The Showcase Card */
    .showcase-card {
      background: var(--glass);
      backdrop-filter: blur(20px);
      width: 100%; max-width: 1100px;
      border-radius: 40px;
      border: 1px solid rgba(255, 255, 255, 0.5);
      box-shadow: 0 50px 100px -20px rgba(0, 0, 0, 0.1);
      display: grid; grid-template-columns: 1fr 1.2fr;
      overflow: hidden;
    }

    /* Left: Image Spotlight */
    .image-spotlight {
      background: white; padding: 60px;
      display: flex; align-items: center; justify-content: center;
    }
    .image-spotlight img {
      max-width: 100%; height: auto;
      filter: drop-shadow(0 20px 40px rgba(0,0,0,0.1));
    }

    /* Right: Content Area */
    .content-area { padding: 60px; display: flex; flex-direction: column; }

    .badge {
      padding: 6px 16px; background: var(--primary-soft, #ccfbf1);
      color: var(--primary); border-radius: 100px;
      font-size: 0.7rem; font-weight: 800; letter-spacing: 1px;
      text-transform: uppercase; margin-bottom: 16px; width: fit-content;
    }

    .title { font-size: 3rem; font-weight: 800; margin-bottom: 10px; letter-spacing: -2px; }
    
    .formulation { 
      font-size: 1.1rem; color: var(--text-muted); 
      margin-bottom: 30px; display: flex; align-items: center; gap: 8px;
    }

    /* AI Description Box */
    .ai-section {
      margin-top: 20px; padding: 24px;
      background: rgba(255, 255, 255, 0.5);
      border: 1px solid rgba(13, 148, 136, 0.1);
      border-radius: 24px; position: relative;
    }

    #description-text {
      font-size: 1rem; line-height: 1.6; color: #475569;
      min-height: 60px; transition: 0.3s;
    }

    .btn-ai {
      margin-top: 15px; background: var(--brand-dark);
      color: white; border: none; padding: 12px 20px;
      border-radius: 14px; font-weight: 700; cursor: pointer;
      display: flex; align-items: center; gap: 8px;
      transition: 0.3s; font-size: 0.9rem;
    }
    .btn-ai:hover { background: var(--primary); transform: translateY(-2px); }
    
    /* Loading Animation */
    .loading-shimmer {
      background: linear-gradient(90deg, #f1f5f9 25%, #e2e8f0 50%, #f1f5f9 75%);
      background-size: 200% 100%;
      animation: shimmer 1.5s infinite;
      border-radius: 4px; color: transparent !important;
    }

    @keyframes shimmer { 0% { background-position: 200% 0; } 100% { background-position: -200% 0; } }

    .price-tag { font-size: 2.5rem; font-weight: 800; color: var(--brand-dark); margin: 30px 0; }

    @media (max-width: 900px) {
      .showcase-card { grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>

  <a href="/user/Medcines" class="back-nav">
    <i class="ph-bold ph-arrow-left"></i> COLLECTION
  </a>

  <div class="showcase-card">
    <div class="image-spotlight">
      <img src="<%= mtv.imageUrl %>" alt="<%= mtv.name %>">
    </div>

    <div class="content-area">
      <span class="badge">Pharmaceutical Grade</span>
      <h1 class="title"><%= mtv.name %></h1>
      <p class="formulation"><i class="ph ph-flask"></i> <%= mtv.form %></p>

      <div class="ai-section">
        <div id="description-text">
          No clinical description available yet. Use the AI assistant to generate one.
        </div>
        
        <button id="aiTrigger" class="btn-ai" onclick="">
          <i class="ph-fill ph-sparkle"></i>
          <span>Generate AI Description</span>
        </button>
      </div>

      <div class="price-tag">$<%= mtv.price %></div>
      <% if (role==='ADMIN') { %>
       <div style="display: flex; gap: 12px;">
         <a href="/edit/<%= mtv._id %>" style="text-decoration: none; flex: 1; text-align: center; padding: 18px; border-radius: 20px; border: 1px solid var(--border); font-weight: 700; color: var(--brand-dark);">Edit Entry</a>
         <button style="flex: 2; background: var(--primary); color: white; border: none; padding: 18px; border-radius: 20px; font-weight: 700; cursor: pointer;">Add to Inventory</button>
      </div>
    </div>
  </div>
      <% } %>
      

  <script>
    const aiBtn = document.getElementById('aiTrigger');
    const descText = document.getElementById('description-text');

    aiBtn.addEventListener('click', async () => {
      // Start Loading State
      aiBtn.disabled = true;
      aiBtn.innerHTML = `<i class="ph ph-circle-notch animate-spin"></i> Analyzing...`;
      descText.classList.add('loading-shimmer');

      try {
        // Calling your AI backend route
        const response = await fetch('/admin/generate-ai/<%= mtv._id %>', {
        method: 'POST' // Matches your router.post
    });
        const data = await response.json();
        descText.classList.remove('loading-shimmer');
        descText.innerText = data.description;
        
        // Success: Show the AI text
        descText.classList.remove('loading-shimmer');
        descText.innerText = data.description;
        aiBtn.innerHTML = `<i class="ph ph-check-circle"></i> Regenerate`;
      } catch (err) {
        descText.classList.remove('loading-shimmer');
        descText.innerText = "Error connecting to AI. Please try again.";
        aiBtn.innerHTML = `<i class="ph ph-warning"></i> Try Again`;
      } finally {
        aiBtn.disabled = false;
      }
    });
  </script>

</body>
</html>