<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Details of <%= mtv.name %></title>
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
  <style>
    :root {
      --brand: #1abc9c;
      --brand-dark: #148f77;
    }

    body {
      background-color: #f6f6f6;
    }

    nav {
      background-color: var(--brand);
    }

    .detail-card {
      border-radius: 16px;
      padding: 2.5rem 2rem;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08);
      animation: fadeUp 0.6s ease;
    }

    .detail-image {
      width: 100%;
      border-radius: 14px;
      margin-bottom: 1.5rem;
      box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
    }

    .price-chip {
      background: rgba(26, 188, 156, 0.15);
      color: #1a7f6b;
      font-weight: 600;
    }

    .btn {
      background-color: var(--brand);
    }

    .btn:hover {
      background-color: var(--brand-dark);
    }

    @keyframes fadeUp {
      from { transform: translateY(16px); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }
  </style>
</head>
<body>
  <nav>
    <div class="nav-wrapper container">
      <a href="/" class="brand-logo">💊 PharmaPlus</a>
      <ul class="right hide-on-med-and-down">
        <li><a href="/user/Medcines">Inventory</a></li>
        <li><a href="/logout">Logout</a></li>
      </ul>
    </div>
  </nav>

  <main class="container section">
    <div class="row">
      <div class="col s12 m10 l8 offset-m1 offset-l2">
        <div class="detail-card white center-align">
          <% if (mtv.imageUrl) { %>
            <img class="detail-image" src="<%= mtv.imageUrl %>" alt="Medicine image">
          <% } %>
          <h4><%= mtv.name %></h4>
          <p class="flow-text"><strong>Form:</strong> <%= mtv.form %></p>
          <span class="new badge price-chip" data-badge-caption="$"><%= mtv.price %></span>
          <div class="section"></div>
          <a href="/user/Medcines" class="btn waves-effect waves-light">
            <i class="material-icons left">arrow_back</i>
            Back to Inventory
          </a>
        </div>
      </div>
    </div>
  </main>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>
</html>
